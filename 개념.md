## 향상 for 문 

```
for (타입 변수명 : 배열 또는 컬렉션) {
    // 반복해서 실행할 코드
}


int[] numbers = {1, 2, 3, 4, 5};

for (int number : numbers) {
    System.out.println(number);
}


```

## 15:00 이라고 나타날 떄 : 을 기준으로 앞 뒤 자르는방법 (시간관련 문제 정수변환 *60)

```
• String의 split(":")을 사용하면 :을 기준으로 나눌 수 있습니다.

  String time = "15:00";
  String[] parts = time.split(":");

  String hour = parts[0];   // "15"
  String minute = parts[1]; // "00"

  System.out.println(hour);
  System.out.println(minute);

  숫자로 계산하려면 Integer.parseInt()로 바꾸면 됩니다.

  int hour = Integer.parseInt(parts[0]);     // 15
  int minute = Integer.parseInt(parts[1]);  // 0
  int totalMinutes = hour * 60 + minute;    // 900

  호텔 예약 시간처럼 비교하거나 계산할 때는 시각을 분 단위 정수로 바꾸면 편합니다.

  ```